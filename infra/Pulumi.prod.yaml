config:
  aws:region: us-east-1
  # Set at Phase D (after the prod Railway service exists), NOT committed:
  #   pulumi config set eq-live-transcription-prod:granola_cron_endpoint https://<prod-railway-host>/internal/granola/cron-tick
  #   pulumi config set --secret eq-live-transcription-prod:cron_secret <INTERNAL_CRON_SECRET>
  # Until granola_cron_endpoint is set, only the S3 upload bucket is provisioned
  # (Phase B applies S3 only; the EventBridge poll chain rides Phase D).
