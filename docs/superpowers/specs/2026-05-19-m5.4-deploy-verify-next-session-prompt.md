# Next Session Opening Prompt (2026-05-19, M5.4 deploy + verify arc)

Paste the block below as the opening message of the next Claude session.

Written 2026-05-18 end-of-day after M5.4 code work + 5-round Codex review + 1 adversarial Codex challenge completed locally. M5.4 commits are on branch `phase-1/m5.4-neo4j-merge-key-alignment` in `/Users/peteroneil/eq-email-pipeline` — **UNPUSHED, awaiting push + PR + merge auth.** The session that produced this prompt explicitly checkpointed here (Path 1 per user direction: complete code+review in one session, deploy+verify+sign-off in a fresh session) to give the deploy arc a clean context window.

---

```
You're picking up the Contact Quality and Account-Anchoring Initiative —
a multi-phase data-quality project on an AI-native customer intelligence
platform.

═══════════════════════════════════════════════════════════════════════
PROJECT TRAJECTORY (where we are in the multi-phase plan)
═══════════════════════════════════════════════════════════════════════

  - **Phase 1.5** (DBOS async-orchestration substrate): DONE. Shipped
    + verified in PRs #14-#18 (2026-05-15→2026-05-17).
  - **Phase 1-email-pipeline** (cold-inbound capture): IN PROGRESS.
    M1, M2, M3, M4, M5.1, M5.2, M5.3 shipped + deployed. M5.4 is
    coded + reviewed + tested locally on branch
    `phase-1/m5.4-neo4j-merge-key-alignment` in eq-email-pipeline,
    UNPUSHED. **This session ships M5.4 the rest of the way: push →
    PR → merge auth → deploy verify → §10.3 E2E walk → §11 invariants
    → Phase 1 sign-off.**
  - **Phase 2**: gated on Phase 1 sign-off. Separate future session.

═══════════════════════════════════════════════════════════════════════
WHAT M5.4 ALREADY SHIPPED LOCALLY (the work to deploy)
═══════════════════════════════════════════════════════════════════════

5 commits on `phase-1/m5.4-neo4j-merge-key-alignment` in eq-email-pipeline:

  - `6a61254` M5.4 main fix — replaced MERGE-on-IMID + CREATE-fallback
    with single MERGE on `(tenant_id, interaction_id)` + ON MATCH SET
    COALESCE pattern. Added `interaction_unique` constraint to
    `ensure_constraints` (idempotent in prod). 9 tests + plan §17 addendum.
  - `77129af` Codex R1 P2 fold — test isolation: `_count_interactions_by_id`
    helper replaces tenant-wide count in the second-writer integration test.
  - `76cf2f2` Codex R2 2×P2 fold — `source` and `content_text` switched
    from COALESCE to unconditional overwrite on match (eq-email-pipeline
    is authoritative for emails). New integration test verifies overwrite
    against eq-structured-graph-core's 'api' source placeholder.
  - `78e3ced` Codex challenge finding #1 fold — discovered 2 more
    Interaction writers (eq-interaction-threads, thematic-lm) that create
    defensive stubs without interaction_type or timestamp. Added COALESCE-
    backfill for those fields on match + `timestamp` on ON CREATE SET.
    New stub-first integration test.
  - `dbd73c4` Codex R4 P1 fold — conftest.py reads
    `neo4j_container.username/password` instead of hardcoding, so
    environments with NEO4J_PASSWORD env var don't break the containerized
    suite.

Codex review trajectory:
  R1 P2 → fold → R2 2×P2 → fold → R3 CLEAN → challenge → fold + Phase 2
  defers (Findings #2 #3) → R4 P1 → fold → R5 CLEAN.

Test status:
  11/11 M5.4 tests pass (7 Cypher-shape AsyncMock unit tests + 4 multi-
  writer integration tests against Testcontainers Neo4j: second-writer-
  enriches, stub-first-backfills, split-brain-legacy-failure, both-
  constraints-honored). 479 other tests pass. 3 pre-existing failures in
  test_pipeline_integration.py are FK-violation environmental issues
  unrelated to M5.4 (verified on bare main).

═══════════════════════════════════════════════════════════════════════
M5.4 FIX DETAILS (cliff notes — full design in plan §17)
═══════════════════════════════════════════════════════════════════════

Production Neo4j has 2 UNIQUENESS constraints on `:Interaction`:
  - `interaction_unique` on (tenant_id, interaction_id) — the canonical
    key all writers MERGE on
  - `interaction_email_dedup` on (tenant_id, internet_message_id) —
    belt-and-suspenders; not the MERGE key

Pre-M5.4 bug: eq-email-pipeline MERGEd on (tenant_id, internet_message_id).
When eq-structured-graph-core / action-item-graph / eq-interaction-threads /
thematic-lm created the node first by (tenant_id, interaction_id) without
IMID, the MERGE couldn't reconcile → CREATE-fallback → interaction_unique
violation → handler raises → SQS DLQ.

M5.4 fix: single MERGE on (tenant_id, interaction_id) with 3-class ON
MATCH SET:
  (a) authoritative-overwrite (source, content_text) — eq-email-pipeline
      canonical for emails
  (b) stub-writer-backfill (interaction_type, timestamp) — COALESCE
      backfills NULL values
  (c) email-only COALESCE (subject, from_email, direction, thread_key,
      has_attachments, provider_message_id, internet_message_id,
      connected_user_id, sent_at) — preserve earlier eq-email-pipeline
      run's values across retries

Files touched (eq-email-pipeline ONLY):
  - src/pipeline/skeleton.py
  - src/persistence/neo4j.py (added interaction_unique constraint)
  - src/pipeline/email_promoted_subscriber.py (comment refresh only)
  - tests/conftest.py (Neo4jContainer auth fix)
  - tests/test_skeleton_multi_writer.py (NEW; 11 tests)
  - docs/superpowers/plans/2026-05-17-pending-interactions-cold-inbound-fix.md
    (§17 addendum)

NO other repos touched: eq-structured-graph-core, action-item-graph,
live-transcription-fastapi, eq-frontend, eq-interaction-threads,
thematic-lm, opportunity-forecasting — all preserved.

═══════════════════════════════════════════════════════════════════════
SESSION SCOPE
═══════════════════════════════════════════════════════════════════════

IN scope:
  1. Pre-flight verification (health, branch state, DBOS drain, test
     tenant baseline, NULLS NOT DISTINCT, SQS state, LOCKED-17).
  2. ASK USER: push the branch.
  3. Open PR with full Codex-review trajectory + test plan + deploy plan.
  4. ASK USER: merge authorization (LOCKED-10 + per-initiative auth not
     extending).
  5. After merge: wait for Railway deploy SUCCESS + /api/health 200.
  6. ASK USER: drain the M5.3 leftover DLQ message (1 message;
     interaction_id c1a7a5ac-... points to already-deleted data).
  7. Re-run plan §10.3 Steps 1-12 on a FRESH UUID against test tenant
     11111111-1111-4111-8111-111111111111. Step 8 (emails.local_enrichment_completed_at
     NOT NULL within 3-5 min) is the M5.4-specific verification.
  8. Walk plan §11 22-item invariants checklist.
  9. (OPTIONAL — ASK USER) plan §10.4 rollback drill.
  10. Phase-1-email-pipeline INITIATIVE COMPLETE sign-off.
  11. End-of-session handoff: /context-save, rewrite NEXT-SESSION-START-
      HERE.md for Phase 2 brainstorming, write a new next-session prompt,
      update MEMORY.md, commit + push handoff docs.

OUT of scope:
  - Any new feature work or refactor beyond what M5.4 already shipped.
  - Phase 2 work (Neo4j MERGE-everywhere refactor, contact identity
    state machine, outbound, queue UI, etc.).
  - Codex challenge findings #2 + #3 (already documented as Phase 2
    deferrals in plan §17.4).
  - Modifying any of the M5.4 commits on the branch.

If Step 8 of §10.3 STILL stalls after deploy → 6th bug. STOP, surface
to user, treat as M5.5 (or escalate).

═══════════════════════════════════════════════════════════════════════
MANDATORY STEPS — IN ORDER — BEFORE ANY DEPLOY WORK
═══════════════════════════════════════════════════════════════════════

1. Run `/context-restore`. You should find a checkpoint titled
   "phase-1-email-pipeline-m5.4-coded-reviewed-ready-to-push" dated
   2026-05-18 end-of-day in
   `~/.gstack/projects/oneilstokeseqrm-live-transcription-fastapi/
   checkpoints/`. Load it.

2. Confirm MEMORY.md status reads
   `PHASE_1_EMAIL_PIPELINE_M5.4_CODED_REVIEWED_READY_TO_PUSH`.
   If anything else, STOP and surface.

3. **READ THE PLAN §17 ADDENDUM** (load-bearing M5.4 design):
   /Users/peteroneil/eq-email-pipeline/docs/superpowers/plans/
   2026-05-17-pending-interactions-cold-inbound-fix.md (§17).

4. **READ THE WAYFINDING DOC** (full deploy-verify scope, STOP
   conditions, references):
   /Users/peteroneil/EQ-CORE/live-transcription-fastapi/docs/superpowers/
   specs/NEXT-SESSION-START-HERE.md.

5. READ THE M5.4 BRANCH (5 commits):
   ```
   git -C /Users/peteroneil/eq-email-pipeline log --oneline main..phase-1/m5.4-neo4j-merge-key-alignment
   ```
   Expected: dbd73c4 → 78e3ced → 76cf2f2 → 77129af → 6a61254.

6. Read the M5.4 evidence file (root-cause forensics from M5.3 session;
   still load-bearing for context):
   /Users/peteroneil/EQ-CORE/live-transcription-fastapi/tasks/
   m5.4-bug-evidence.md.

7. Read the 2 new feedback memories from M5.4 design session:
   - feedback_test_pattern_no_docker.md (test pattern is AsyncMock
     unit tests + production E2E; Docker/Testcontainers exists but is
     not the default workflow)
   - feedback_complete_all_handoff_reads_before_action.md (read EVERY
     mandatory artifact before pre-flight or action)

8. PRE-FLIGHT VERIFICATION (per wayfinding §"Pre-flight for the deploy
   session"). Health + branch state + collision check + DBOS drain +
   tenant baseline + NULLS NOT DISTINCT + SQS state. If any fails,
   STOP and surface.

9. After reading + pre-flight passing, write ONE paragraph confirming
   the state: branch ready, pre-flight clean, intent to push + PR +
   ASK merge auth. Then proceed.

10. Execute the deploy-verify arc per Items 1-11 in SESSION SCOPE above.

═══════════════════════════════════════════════════════════════════════
USER POSTURE (load-bearing)
═══════════════════════════════════════════════════════════════════════

Non-developer founder. Make confident technical decisions; surface only
product/strategic decisions. Strict OSS only.

User explicitly approves git push when asked; do NOT push without
asking first. Same for production-affecting Railway env var changes.
The rollback drill IS reversible; ask before running it. Per-initiative
merge auth doesn't carry forward — ASK FIRST.

User's rules (load-bearing):
  1. Complete Phase N before Phase N+1 planning. Phase 1 must close
     clean before Phase 2 begins.
  2. Cutting-edge-startup approach. No shortcuts unless the shortcut IS
     the correct architecture.
  3. AI agent doesn't merge PRs without per-initiative authorization.
     M5.3's auth does NOT extend; ASK for M5.4.
  4. Plain-English explanations when user asks "why" / "what happened".
  5. Don't break other repos. M5.4 fix is scoped to eq-email-pipeline
     ONLY; verify no regressions in transcript pipeline or other
     consumers.
  6. Investigate thoroughly; use the right gstack skills.
  7. Don't go beyond scope. M5.4 finishes Phase 1; Phase 2 is later.

═══════════════════════════════════════════════════════════════════════
STOP CONDITIONS (hard — surface to user)
═══════════════════════════════════════════════════════════════════════

- /context-restore returns NO_CHECKPOINTS or wrong checkpoint.
- MEMORY.md status isn't PHASE_1_EMAIL_PIPELINE_M5.4_CODED_REVIEWED_READY_TO_PUSH.
- Production /api/health returns non-200 anywhere.
- Branch phase-1/m5.4-neo4j-merge-key-alignment doesn't exist or has
  unexpected commits.
- LOCKED-17 collision check shows concurrent agent.
- §10.3 Step 8 STILL stalls after M5.4 fix deploys → 6th bug.
- Codex review post-merge surfaces a P0/P1 finding.
- E2E surfaces a NEW V1 limitation not in the documented 4.

═══════════════════════════════════════════════════════════════════════
KEY REFERENCE PATHS (quick reference)
═══════════════════════════════════════════════════════════════════════

WAYFINDING:
  /Users/peteroneil/EQ-CORE/live-transcription-fastapi/docs/superpowers/
  specs/NEXT-SESSION-START-HERE.md

THE PLAN (§17 is the M5.4 design):
  /Users/peteroneil/eq-email-pipeline/docs/superpowers/plans/
  2026-05-17-pending-interactions-cold-inbound-fix.md

M5.4 BRANCH:
  /Users/peteroneil/eq-email-pipeline (branch
  phase-1/m5.4-neo4j-merge-key-alignment, 5 commits)

EVIDENCE FILE:
  /Users/peteroneil/EQ-CORE/live-transcription-fastapi/tasks/
  m5.4-bug-evidence.md

LESSONS:
  /Users/peteroneil/EQ-CORE/live-transcription-fastapi/tasks/lessons.md

NEW FEEDBACK MEMORIES (this session):
  ~/.claude/projects/-Users-peteroneil-EQ-CORE-live-transcription-
  fastapi/memory/feedback_test_pattern_no_docker.md

═══════════════════════════════════════════════════════════════════════

Start with /context-restore. Then the mandatory reads. Then pre-flight.
Then ASK USER to push the branch. Then PR + merge auth + deploy verify
+ §10.3 E2E + §11 invariants + Phase 1 sign-off + handoff.

The session is ~2-3 hours of focused work. Smaller than M5.4-design-
session because the heavy lifting is done; this is deploy + verify.
```

---

## Notes for the user pasting this

This prompt is the M5.4 deploy + verify + Phase 1 sign-off arc handoff. Key differences vs the M5.4-design-session prompt:

- **Status string is `PHASE_1_EMAIL_PIPELINE_M5.4_CODED_REVIEWED_READY_TO_PUSH`**. The fix is committed locally; this session ships it.
- **The session scope is deploy + verify, not design + code.** The 5 commits on the branch are intentionally not modifiable; the next session's job is to push them through to production and verify end-to-end.
- **5 rounds of Codex review + 1 adversarial challenge already done.** Don't re-run them unless something surfaces post-deploy.
- **Phase 1 sign-off is the terminal state.** After §10.3 + §11 PASS, the entire Phase-1-email-pipeline initiative is COMPLETE. Phase 2 PLANNING is a separate future session.
- **Expected runtime: ~2-3 hours.** Deploy is fast; the E2E walk + §11 invariants are the slower parts.
